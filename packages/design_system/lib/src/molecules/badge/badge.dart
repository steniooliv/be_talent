import 'package:design_system/src/molecules/badge/badge_component.dart';

class BeBadge extends BadgeComponent {
  const BeBadge.standard({
    super.key,
    super.icon,
    super.color,
    super.iconSize,
    super.label,
  });
}
